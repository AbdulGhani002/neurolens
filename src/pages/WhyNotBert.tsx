import PageShell from "../components/PageShell";

export default function WhyNotBert() {
  return (
    <PageShell
      eyebrow="Module · 06"
      title="Why not BERT for generation?"
      subtitle="An interactive demonstration of where the encoder-only architecture breaks for generation tasks."
    >
      <div className="glass rounded-2xl p-10 text-ink-300">Coming up next — failure-mode demo.</div>
    </PageShell>
  );
}
