import PageShell from "../components/PageShell";

export default function Tokenizer() {
  return (
    <PageShell
      eyebrow="Module · 03"
      title="Tokenizer Playground"
      subtitle="WordPiece, SentencePiece, and BPE — see them break sentences differently."
    >
      <div className="glass rounded-2xl p-10 text-ink-300">Coming up next — split panes.</div>
    </PageShell>
  );
}
